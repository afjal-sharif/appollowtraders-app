name: Vehicle Expiry Alert Check

on:
  schedule:
    - cron: "0 4 * * *"
  workflow_dispatch:

jobs:
  run-alert-check:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger secure cron endpoint
        run: |
          curl -L "https://tests.cdndirect.workers.dev/cron-check?token=apollo-cron-2026-secure"
